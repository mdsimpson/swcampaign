export default function VerifyEmail(){
  return (
    <div style={{maxWidth:700,margin:'40px auto'}}>
      <h2>Email Verification</h2>
      <p>This app uses Cognito’s verification. Follow the email link/code, then <a href='/landing'>log in</a>.</p>
    </div>
  )
}
